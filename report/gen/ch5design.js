const { h1, h2, h3, para, bullet, table, img, caption } = require('./util');

function ch5Design() {
  return [
    h1('CHAPTER 5: SYSTEM DESIGN'),
    h2('5.1 Design Overview'),
    para('The system follows a classic three-tier architecture: a React single-page application (presentation tier), an Express.js REST API (application tier), and a swappable persistence layer (data tier). The presentation tier communicates with the application tier exclusively through JSON over HTTP, with a JWT bearer token attached to every protected request. The application tier validates the token, checks role permissions, executes business logic, and delegates persistence to a unified database interface that hides whether the underlying store is a set of JSON files or a MongoDB database.'),
    para('This chapter documents the design through standard modelling artefacts: the system architecture diagram, use case diagram, data flow diagrams (levels 0, 1, and 2), the entity-relationship model, the class diagram, sequence diagrams for the four most important workflows, an activity diagram for the admission process, and the physical database design.'),
    h2('5.2 System Architecture'),
    para('The architecture diagram below shows the three tiers and their internal components. The client bundles React Router for navigation, a global application context for authentication and theme state, and an Axios instance whose interceptors attach the JWT and handle expiry. The server stacks middleware (CORS, JSON parsing, authentication, role authorization) in front of more than thirty route modules, which share a CRUD factory and central business-logic helpers. The data layer exposes one interface with two interchangeable implementations.'),
    img('architecture.png', { maxH: 7.6 }),
    caption('Figure 5.1: Three-tier system architecture of the School Management System'),
    h2('5.3 Use Case Diagram'),
    para('The use case diagram identifies the six actors and the principal system functions available to each. All actors share the authentication use case. Administrative use cases (publishing results, payroll generation, settings) are reserved for the Admin; operational use cases (admissions, fee collection, inventory, library circulation) belong to the Clerk and Supervisor; academic use cases (attendance, marks entry) belong to Teachers; and self-service use cases belong to Students and Parents.'),
    img('usecase.png', { maxH: 7.6 }),
    caption('Figure 5.2: Use case diagram showing actors and their system interactions'),
    h2('5.4 Data Flow Diagrams'),
    h3('5.4.1 DFD Level 0 (Context Diagram)'),
    para('At the context level, the entire application is a single process exchanging data flows with five external entities — the user roles. Inputs include credentials, application data, attendance markings, marks, and payments; outputs include dashboards, receipts, report cards, hall tickets, and alerts.'),
    img('dfd-level0.png'),
    caption('Figure 5.3: DFD Level 0 — context diagram'),
    h3('5.4.2 DFD Level 1'),
    para('Level 1 decomposes the system into eight major processes and eight data stores. Note how the fee and finance process (6.0) receives postings from both the library process (7.0, fines) and the payroll process (8.0, salary expenses), reflecting the integrated accounting design. The admission process (2.0) writes login records to the users store (D1) when an applicant is enrolled.'),
    img('dfd-level1.png', { maxH: 7.8 }),
    caption('Figure 5.4: DFD Level 1 — major processes and data stores'),
    h3('5.4.3 DFD Level 2 (Fee Collection Process)'),
    para('The fee collection process is expanded to level 2 because it contains the most business logic. Due computation (6.1) joins three stores — the student\u2019s class and transport assignment, the class fee structure, and previously paid months — to produce the monthly due. Payment recording (6.2) obtains an atomic receipt sequence number, receipt generation (6.3) produces the printable document, and posting (6.4) writes the income entry to daily accounts. The outstanding report (6.5) aggregates unpaid months across all active students.'),
    img('dfd-level2-fee.png', { maxH: 7.2 }),
    caption('Figure 5.5: DFD Level 2 — decomposition of the fee and finance process'),
    h2('5.5 Entity-Relationship Diagram'),
    para('The ER model contains eighteen principal entities. Cardinalities follow crow\u2019s-foot notation: a Parent has zero or more Students (siblings link to one parent record); a Class enrolls many Students and offers many Subjects; an Exam contains many Marks records, each of which references one Student and one Subject; a Book is issued through many Book Issues, each tied to one Student; Receipts and Salary Slips each post entries into Daily Accounts. User login records optionally link to a Student or Parent, enabling the portal views.'),
    img('er-diagram.png', { maxH: 8.6 }),
    caption('Figure 5.6: Entity-relationship diagram of the complete database'),
    h2('5.6 Class Diagram'),
    para('The class diagram models the domain from an object-oriented perspective. User is the generalization of the role-bearing actors (Teacher, Parent, Student are shown as specializations through their role). Aggregations connect Parent to Student (children) and SchoolClass to Student and Subject. Exam is composed of MarkSheets, which move through draft, submitted, and locked states. FeeReceipt and SalarySlip both collaborate with DailyAccount for ledger postings, and BookIssue encapsulates fine calculation and return processing.'),
    img('class-diagram.png', { maxH: 8.6 }),
    caption('Figure 5.7: Class diagram of the domain model'),
    h2('5.7 Sequence Diagrams'),
    h3('5.7.1 User Authentication Sequence'),
    para('The login sequence shows credential verification with bcrypt, JWT issuance, and the role-check path taken by every subsequent request through the authentication middleware.'),
    img('seq-login.png', { maxH: 8.0 }),
    caption('Figure 5.8: Sequence diagram — login and authenticated request handling'),
    h3('5.7.2 Fee Payment Sequence'),
    para('The fee payment sequence covers due computation, receipt persistence with an atomic sequence number, the automatic income posting to daily accounts, and receipt printing.'),
    img('seq-fee.png', { maxH: 7.6 }),
    caption('Figure 5.9: Sequence diagram — fee due computation and receipt generation'),
    h3('5.7.3 Examination Workflow Sequence'),
    para('The examination sequence involves two actors. The teacher retrieves the class sheet, saves drafts, and submits; the administrator reviews submitted sheets and publishes, after which results become visible to students and parents.'),
    img('seq-exam.png', { maxH: 7.6 }),
    caption('Figure 5.10: Sequence diagram — marks entry, submission, and publishing'),
    h3('5.7.4 Library Circulation Sequence'),
    para('The library sequence shows availability checking at issue time, automatic due-date assignment, overdue fine computation at return time, and the posting of collected fines to daily accounts.'),
    img('seq-library.png', { maxH: 7.8 }),
    caption('Figure 5.11: Sequence diagram — book issue, return, and fine posting'),
    h2('5.8 Activity Diagram — Admission Workflow'),
    para('The activity diagram traces an application from submission through review, the approve/reject decision, enrollment (roll number, login creation, optional transport), parent linking, and final confirmation that activates the student across all modules.'),
    img('activity-admission.png', { maxH: 8.4 }),
    caption('Figure 5.12: Activity diagram — admission and enrollment workflow'),
    h2('5.9 Database Design'),
    para('The physical schema consists of MongoDB-style document collections. In file mode each collection is a JSON file with an identical logical structure, which is what makes the store swappable. The most important collections and their fields are tabulated below. Every document carries a unique string _id generated by nanoid, and human-facing serial numbers (admission number, receipt number, accession number, slip number) come from an atomic counters collection.'),
    h3('5.9.1 users'),
    table([
      ['Field', 'Type', 'Description'],
      ['_id', 'string', 'Primary key (nanoid)'],
      ['name', 'string', 'Display name'],
      ['email', 'string', 'Login identifier (unique)'],
      ['passwordHash', 'string', 'bcrypt hash, never returned by APIs'],
      ['role', 'string', 'admin | clerk | supervisor | teacher | student | parent'],
      ['linkedStudentId', 'string', 'FK → students (for student logins)'],
      ['linkedParentId', 'string', 'FK → parents (for parent logins)'],
    ], [24, 16, 60]),
    h3('5.9.2 students'),
    table([
      ['Field', 'Type', 'Description'],
      ['_id', 'string', 'Primary key'],
      ['admissionNo', 'string', 'Serial e.g. 2024-00000001 (from counter)'],
      ['name', 'string', 'Full name'],
      ['classId', 'string', 'FK → classes'],
      ['parentId', 'string', 'FK → parents'],
      ['rollNo', 'number', 'Roll number within class'],
      ['dob', 'date', 'Date of birth (drives birthday widget)'],
      ['gender / house', 'string', 'Demographics and house allocation'],
      ['transportRoute', 'string', 'Optional; adds transport fee when set'],
      ['medicalNotes', 'string', 'Allergies and health restrictions'],
      ['status', 'string', 'active | inactive | transferred | passed_out | suspended'],
    ], [24, 16, 60]),
    h3('5.9.3 classes, subjects, teachers'),
    table([
      ['Collection', 'Key Fields', 'Notes'],
      ['classes', 'className, section, academicYear, capacity, room, classTeacherId', 'className+section+academicYear unique'],
      ['subjects', 'code, name, maxMarks, passMarks, classId', 'Assigned per class'],
      ['teachers', 'name, email, phone, qualification, baseSalary', 'baseSalary feeds payroll'],
    ], [20, 45, 35]),
    h3('5.9.4 attendance'),
    table([
      ['Field', 'Type', 'Description'],
      ['studentId', 'string', 'FK → students'],
      ['classId', 'string', 'FK → classes'],
      ['date', 'date', 'Attendance date'],
      ['status', 'string', 'present | absent | late | halfday | leave'],
    ], [24, 16, 60]),
    h3('5.9.5 exams and marks'),
    table([
      ['Collection', 'Key Fields', 'Notes'],
      ['exams', 'name, classId, startDate, status', 'status: draft | active | published'],
      ['marks', 'examId, studentId, subjectId, marksObtained, sheetStatus', 'sheetStatus: draft | submitted | locked'],
    ], [20, 45, 35]),
    h3('5.9.6 fee and accounting collections'),
    table([
      ['Collection', 'Key Fields', 'Notes'],
      ['feeStructures', 'classId, head, amount, frequency', 'Defines tuition/transport/other heads'],
      ['receipts', 'receiptNo, studentId, amount, payMode, months, paidOn', 'receiptNo from atomic counter'],
      ['dailyAccounts', 'date, type (income|expense), category, amount, refNo', 'Receives postings from fees, fines, payroll'],
    ], [20, 45, 35]),
    h3('5.9.7 library collections'),
    table([
      ['Collection', 'Key Fields', 'Notes'],
      ['books', 'accNo, title, author, isbn, category, shelf, copies, availableCopies, coverImage', 'accNo e.g. LIB-00001; cover stored as data URL'],
      ['bookIssues', 'bookId, studentId, issueDate, dueDate, returnDate, fine, status', 'fine = daysLate × finePerDay on return'],
    ], [20, 48, 32]),
    h3('5.9.8 payroll and other collections'),
    table([
      ['Collection', 'Key Fields', 'Notes'],
      ['salarySlips', 'slipNo, teacherId, month, allowances[], deductions[], gross, netPay, status', 'Paying posts expense to dailyAccounts'],
      ['admissions', 'admissionNo, applicantName, classApplied, status, rejectReason', 'Approval workflow source'],
      ['parents', 'name, phone, email, occupation', 'Linked from students.parentId'],
      ['assets / inventory', 'tag, name, location, maintenance[] / item, qty, reorderLevel, unit', 'Stock in / issue / adjust movements'],
      ['notices / timetable / lessonPlans / logbook / discipline / helpdesk', 'Module-specific fields', 'Communication and tracking modules'],
      ['counters', 'key, value', 'Atomic sequences: admissionNo, receiptNo, bookAcc, salarySlip'],
    ], [22, 46, 32]),
    h2('5.10 Design of the Swappable Data Layer'),
    para('Every route module accesses data exclusively through a col(name) function exported by db/index.js. At startup the module inspects the DB_MODE environment variable: in file mode it returns the file-store implementation, which memory-maps each collection to a JSON file under server/data and persists on every mutation; in mongo mode it returns thin wrappers over the official MongoDB driver connected via MONGO_URI. Both implementations expose the identical contract — insertOne, find with filter/sort/limit, findOne, updateOne, deleteOne, deleteMany, and countDocuments — so business logic is completely unaware of the storage engine. This design satisfies requirement NFR-4 and allows a school to begin with zero infrastructure and migrate to MongoDB Atlas later by exporting the JSON files.'),
  ];
}

module.exports = { ch5Design };
