const { h1, h2, h3, para, bullet, table } = require('./util');

function ch6Technology() {
  return [
    h1('CHAPTER 6: TECHNOLOGY STACK'),
    h2('6.1 The MERN Stack'),
    para('MERN is an acronym for MongoDB, Express.js, React, and Node.js — four technologies that together cover the entire web application stack using a single programming language, JavaScript. Using one language end to end simplifies development, allows sharing of validation logic and conventions between client and server, and gives access to npm, the largest package ecosystem in software development.'),
    h3('6.1.1 MongoDB'),
    para('MongoDB is a document-oriented NoSQL database that stores data as flexible JSON-like documents grouped into collections. Its schema flexibility suits a school system in which entities like students carry many optional fields (medical notes, transport routes, languages) that would produce sparse columns in a relational design. In this project MongoDB is one of two interchangeable storage engines; the hosted MongoDB Atlas service provides the cloud deployment path with automated backups and scaling.'),
    h3('6.1.2 Express.js'),
    para('Express.js is a minimal web framework for Node.js that structures the server as a pipeline of middleware functions and routers. The project uses Express to expose a REST API of more than thirty route modules, with shared middleware handling CORS, JSON body parsing, JWT verification, and role authorization before requests reach business logic.'),
    h3('6.1.3 React'),
    para('React is a component-based library for building user interfaces. The application is a single-page application: React Router swaps page components client-side without full page reloads, and shared state (the logged-in user, school settings, theme) lives in a context provider. The UI is assembled from a reusable component kit — data tables with search/export, KPI cards, modals, status tabs, and form fields — which keeps all forty-plus pages visually consistent.'),
    h3('6.1.4 Node.js'),
    para('Node.js is the JavaScript runtime that executes the server. Its non-blocking, event-driven I/O model handles many concurrent API requests efficiently on modest hardware, which matches the deployment profile of a school office server.'),
    h2('6.2 Supporting Libraries and Tools'),
    table([
      ['Library / Tool', 'Layer', 'Purpose in the Project'],
      ['Vite', 'Client build', 'Development server with hot module replacement and production bundling'],
      ['react-router-dom', 'Client', 'Client-side routing, protected routes, public/portal layouts'],
      ['axios', 'Client', 'HTTP client; interceptors attach JWT and auto-logout on 401'],
      ['chart.js + react-chartjs-2', 'Client', 'Dashboard analytics: attendance, collections, gender mix, severity charts'],
      ['lucide-react', 'Client', 'Consistent icon set across the interface'],
      ['jsonwebtoken', 'Server', 'Signing and verifying JWT access tokens'],
      ['bcryptjs', 'Server', 'One-way password hashing with salt'],
      ['nanoid', 'Server', 'Collision-resistant unique document identifiers'],
      ['cors', 'Server', 'Cross-origin resource sharing between dev servers'],
      ['dotenv', 'Server', 'Environment-based configuration (port, DB mode, JWT secret)'],
      ['MongoDB Node Driver', 'Server', 'Official driver used by the Mongo store implementation'],
    ], [26, 16, 58]),
    h2('6.3 Why MERN for This Project'),
    bullet('Single language across the stack reduces context switching and enables one developer to own a feature end to end.'),
    bullet('JSON everywhere: the database documents, the API payloads, and the client state share the same shape, eliminating mapping layers.'),
    bullet('React\u2019s component model matches the UI requirement of many similar CRUD screens built from one component kit.'),
    bullet('Node.js and the file-based store option allow zero-cost, offline-capable deployment inside a school LAN.'),
    bullet('A clear growth path exists to MongoDB Atlas, container deployment, and horizontal scaling without rewriting the application.'),
  ];
}

function ch7Implementation() {
  return [
    h1('CHAPTER 7: IMPLEMENTATION'),
    h2('7.1 Project Structure'),
    para('The repository is organized as two applications — server and client — plus supporting folders. The server keeps route modules thin by delegating shared behaviour to a CRUD factory, middleware, and the data layer. The client mirrors this with a CRUD page factory, a shared UI kit, and per-module pages.'),
    table([
      ['Path', 'Contents'],
      ['server/src/index.js', 'Express bootstrap: middleware stack and mounting of all API routers'],
      ['server/src/db/', 'Unified data interface (index.js), fileStore.js, mongoStore.js'],
      ['server/src/middleware/auth.js', 'JWT verification and allowRoles(...) role guard'],
      ['server/src/routes/', '30+ route modules: auth, admissions, students, classes, subjects, teachers, attendance, exams, fees, accounts, assets, inventory, library, payroll, search, dashboard, portal, notices, timetable, settings, …'],
      ['server/src/utils/crudFactory.js', 'Generic REST CRUD generator used by simple modules'],
      ['server/src/seed/seed.js', 'Demo data generator for all collections'],
      ['server/data/', 'JSON collections when running in file mode'],
      ['client/src/components/', 'Layout, PublicLayout, CommandPalette, ui.jsx component kit, CrudPage factory'],
      ['client/src/pages/', 'One page per module plus public pages and role dashboards'],
      ['client/src/context/AppContext.jsx', 'Auth/session state, settings, theme persistence'],
      ['client/src/api.js', 'Axios instance with JWT interceptors'],
    ], [34, 66]),
    h2('7.2 Authentication and Role-Based Access Control'),
    para('On login the server verifies the bcrypt hash and signs a JWT embedding the user id and role with an expiry. The client stores the token and attaches it as a Bearer header through an Axios request interceptor; a response interceptor performs automatic logout on any 401. On the server, the auth middleware verifies the signature and attaches req.user, and each route declares its allowed roles through the allowRoles guard — for example, fee collection endpoints accept admin, clerk, and supervisor, while marks entry accepts teachers. Portal endpoints additionally scope queries by the caller\u2019s linked student or parent id so users can only ever read their own records.'),
    h2('7.3 Module Implementation Summaries'),
    h3('7.3.1 Dashboard and Analytics'),
    para('The dashboard route aggregates live counts (active students, teachers, classes, subjects, parents), today\u2019s collection total, outstanding balance, today\u2019s attendance ratio, active exams, and open incidents. Role-specific variants are rendered on the client: the Admin sees approval alerts (pending admissions, mark sheets awaiting publish) and finance charts; the Clerk sees collection-centred KPIs; the Supervisor sees admissions and attendance oversight; the Teacher sees today\u2019s periods and assigned classes. A birthday widget lists students whose birthdays fall in the next seven days, and a greeting bar shows a live clock.'),
    h3('7.3.2 Admissions'),
    para('Applications are captured with applicant, class-applied, academic year, and guardian details, then tracked through Registered, Admitted, and Rejected states. Rejection stores a reason viewable from the list. Enrollment converts an admitted application into a student: it assigns the class and roll number, creates the student login, records the admission date, links or creates the parent record, and optionally assigns a transport route.'),
    h3('7.3.3 Students, Classes, Parents'),
    para('Student profiles include personal, transport, medical, citizenship, and language details, with status tracking across active, inactive, transferred, passed-out, and suspended. Row-level quick actions open fee dues, date-filtered attendance reports, exam results, linked parents, and a printable ID card with house colours and a barcode. Classes enforce the unique combination of name, section, and academic year, and expose a per-class timetable view. Parents are auto-created during registration and can be linked to multiple children.'),
    h3('7.3.4 Attendance'),
    para('A teacher or clerk selects a class and date to load the roll list, then marks each student Present, Absent, Late, Half-day, or Leave with colour-coded toggles. Bulk actions (Mark All Present, Mark All Absent, Copy from Yesterday) accelerate routine days. Saved records feed both the dashboard attendance chart and per-student reports.'),
    h3('7.3.5 Examinations and Results'),
    para('Exams are created per class; teachers enter marks per subject in sheets that move through draft, submitted, and locked states. The Admin reviews submitted sheets and publishes the exam, which unlocks results for students and parents, top-performer and grade-distribution summaries, printable A4 mark sheets and report cards, and hall ticket generation.'),
    h3('7.3.6 Fees, Receipts, and Daily Accounts'),
    para('Fee structures define per-class heads and amounts. At collection time the engine computes dues as tuition plus transport (when assigned) plus late fee minus discounts, across the selected months. Payments accept multiple modes; each saved payment obtains an atomic receipt number and renders a printable receipt that can also be emailed. Every receipt posts an income entry to daily accounts, which also records manual income/expense entries, library fines, and payroll expenses, giving a complete institutional cash view.'),
    h3('7.3.7 Assets and Inventory'),
    para('Assets receive auto-generated tags, locations, and a maintenance history with expenses. Consumable inventory tracks quantities with unit types and reorder levels; Stock In, Issue, and Adjust operations maintain an auditable movement trail with low-stock indicators.'),
    h3('7.3.8 Library Management'),
    para('The catalogue stores accession-numbered books with categories, shelf locations, copy counts, and cover images (uploaded images are resized client-side and validated server-side). The circulation desk issues books with automatic 14-day due dates, decrementing available copies; returns compute fines for late days and post collected fines to daily accounts. Students and parents see their issued books in the portal with due-soon and overdue warnings, and staff see an overdue alert banner with accruing fines.'),
    h3('7.3.9 Payroll'),
    para('Salary slips are generated per staff member per month with configurable allowance and deduction rows; the engine computes gross and net pay. Marking a slip paid posts the expense to daily accounts. Slips render as printable A4 documents, and teachers can view their own slip history.'),
    h3('7.3.10 Communication and Tracking'),
    para('Notices target selectable audiences; the timetable module maintains per-class period grids used by teacher dashboards and substitute planning; lesson planning and logbooks record teaching activity; the discipline module tracks incidents with severity; and the helpdesk manages tickets raised by students and staff.'),
    h3('7.3.11 Global Search and User Experience Features'),
    para('A Ctrl+K command palette performs debounced global search across students, users, parents, receipts, assets, books, exams, and salary slips, with keyboard navigation and direct routing to the matched record. Dark mode is implemented with CSS custom properties toggled by a class on the document root and persisted per user. Public Home, About Us, and Contact pages share a navigation bar that hosts the login form, and logout returns users to the public homepage.'),
    h2('7.4 REST API Summary'),
    table([
      ['Endpoint Group', 'Representative Endpoints', 'Allowed Roles'],
      ['/api/auth', 'POST /login, GET /me', 'All'],
      ['/api/admissions', 'GET /, POST /, PUT /:id, POST /:id/enroll', 'Admin, Clerk, Supervisor'],
      ['/api/students', 'GET /, POST /, PUT /:id, GET /:id/attendance, GET /:id/dues', 'Staff (scoped read for portal)'],
      ['/api/classes, /api/subjects, /api/teachers', 'CRUD + /:id/timetable', 'Admin, Clerk, Supervisor'],
      ['/api/attendance', 'GET /?classId&date, POST /bulk', 'Teacher, Admin, Clerk, Supervisor'],
      ['/api/exams', 'CRUD, GET /:id/sheet, PUT /sheet, POST /:id/publish', 'Teacher (entry), Admin (publish)'],
      ['/api/fees', 'GET /dues/:studentId, POST /receipts, GET /receipts', 'Admin, Clerk, Supervisor'],
      ['/api/accounts', 'GET /, POST /', 'Admin, Clerk'],
      ['/api/assets, /api/inventory', 'CRUD + stock movements', 'Admin, Clerk, Supervisor'],
      ['/api/library', 'CRUD /books, POST /issues, POST /issues/:id/return, GET /my, GET /stats', 'Staff; /my for Student/Parent'],
      ['/api/payroll', 'GET /, POST /, POST /:id/pay', 'Admin, Clerk (own slips for Teacher)'],
      ['/api/search', 'GET /?q=', 'All authenticated (role-scoped results)'],
      ['/api/dashboard', 'GET /stats, GET /teacher', 'Role-dependent'],
      ['/api/portal', 'GET /student/:id/…, GET /parent/children', 'Student, Parent (own data only)'],
      ['/api/settings', 'GET /, PUT /, GET /public/settings', 'Admin (write); public read subset'],
    ], [22, 48, 30]),
    h2('7.5 Security Measures Implemented'),
    bullet('Passwords are stored exclusively as bcrypt hashes; plaintext is never persisted or logged.'),
    bullet('JWTs are signed with a server-side secret and carry an expiry; expired or malformed tokens receive HTTP 401 and trigger client logout.'),
    bullet('Every protected route declares an allow-list of roles; requests from other roles receive HTTP 403.'),
    bullet('Portal queries are filtered server-side by the caller\u2019s linked student/parent id, preventing horizontal privilege escalation.'),
    bullet('Uploaded cover images are size-validated on the server to prevent oversized payloads.'),
    bullet('Serial numbers come from atomic counters, preventing duplicate receipts or admission numbers under concurrency.'),
  ];
}

module.exports = { ch6Technology, ch7Implementation };
