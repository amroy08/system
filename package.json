{
  "name": "complete-school-management-system",
  "version": "1.0.0",
  "private": true,
  "description": "Complete School Management System (MERN, file-based DB switchable to MongoDB Atlas)",
  "scripts": {
    "install-all": "cd server && npm install && cd ../client && npm install",
    "seed": "cd server && npm run seed",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "migrate-mvhs": "node server/src/seed/migrateMVHS.js"
  }
}
